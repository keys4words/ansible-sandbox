-
  name: Play 1
  hosts: web
  tasks:
    - name: Execute command 'date'
      command: date

    - name: Display resolv.conf contents
      command: cat /etc/resolv.conf

    - name: Display resolv.conf contents
      command: cat resolv.conf chdir=/etc

    - name: Ensure to mkake folder 'folder'
      command: mkdir /folder creates=/folder